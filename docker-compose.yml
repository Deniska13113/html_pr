services:
  web_80:
    build: site_80
    ports:
      - "80:80"
      - "443:443"
    restart: always
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt/archive/angel.popovdesigns.uk:/etc/nginx/ssl:ro
  